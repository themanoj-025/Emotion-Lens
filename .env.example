# Emotion-Lens - Environment Template
# Copy to .env if you need overrides. NEVER commit .env.

# API Authentication (optional — leave unset to disable auth)
# EMOTION_API_KEY=your-secret-api-key-here

# Server config
# API_HOST=0.0.0.0
# API_PORT=8000

# Model downloads at runtime via kagglehub (no secrets needed)
